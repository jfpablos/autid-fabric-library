from setuptools import setup, find_packages

setup(
    name="fabric_utils",
    version="0.1.0",
    description="Utilidades compartidas para notebooks de Microsoft Fabric",
    python_requires=">=3.8",
    packages=find_packages(),
)
